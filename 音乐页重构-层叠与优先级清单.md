# 音乐页重构清单：层叠阶梯 · CSS 归属 · 优先级债务

> 本文只写**实测结论**。所有数字来自 Chrome 153 + CDP（`Emulation.setDeviceMetricsOverride`、
> `getComputedStyle`、`elementFromPoint`、CSSOM 读取），六档视口：
> 320×568 / 390×844 / 428×926 / 768×1024 / 820×1180 / 1440×900。
> 不带证据的猜测已剔除（例如体检报告里的「平台胶囊被裁切」「toast 被侧边栏盖住」两条，
> 实测为**误判**，见 §5）。

---

## 1. z-index 阶梯（现状）

| 层级 | 值 | 定义位置 | 实测 |
| --- | --- | --- | --- |
| 迷你播放器 | 9980 | `css/app.css` `html.shell #app-mini-player` | — |
| 顶栏 | 9990 | `css/app.css` | — |
| 底部 tab | 9998 | `css/app.css` | 窄屏 9998；≥769px 隐藏（`auto`） |
| 二级页 detail-view | 9999 | `css/app.css:162` + JS 内联兜底 | 桌面 `[280,0,880,900]` |
| mp 遮罩 | 10000 | `css/app.css:1339` | — |
| **drawer 遮罩** | **10005** | `css/music.css` `--z-drawer-mask` | 六档均为 10005 |
| **drawer** | **10006** | `css/music.css` `--z-drawer` | 六档均为 10006 |
| **toast** | **10010** | `css/music.css` `--z-toast` | 六档均为 10010 |
| **上传弹窗** | **10020** | `css/music.css` `--z-modal` | 六档均为 10020 |
| lightbox | 99999 | `css/style.css:254/392/625` | — |
| 看板娘 | 99999 | `css/mobile.css:485` | 桌面交互框 300×420 |

**本轮已修**：drawer / 遮罩 / toast / 弹窗原先靠 4 条 `!important` 压到 1000–1100 一档，
而它们全都低于 detail-view 的 9999 —— 在二级页里打开歌单抽屉会被压在下面。现在四个值收敛到
`music.css` 的 `:root` token（10005/10006/10010/10020），六档实测一致。

**遗留隐患（未改，需你拍板）**：看板娘是 99999，**高于** toast(10010) 与上传弹窗(10020)。
两者在屏幕上重叠时 toast 会被看板娘盖住。按你「保持现状，不动看板娘」的决定，本轮不动它；
若以后要修，正确做法是**抬高 toast/弹窗**（而不是压低看板娘）。

---

## 2. CSS 归属地图

| 区域 | 文件 | 说明 |
| --- | --- | --- |
| 音乐页基础版式 | `css/music.css`（2043 行） | 播放器卡片、作品网格、解锁区、抽屉 |
| App 外壳（音乐段） | `css/app.css` | **124–1699 行是同一个 `@media (max-width:768px)`**，装着几乎整个音乐 App 层 |
| 桌面音乐扩展 | `css/app.css` | `@media (min-width:769px)` 若干块（含本轮新增的 B1 桌面播放页） |
| 看板娘 / 窄屏兜底 | `css/mobile.css` | 避让规则全部窄屏专属 |
| 层级 token | `css/music.css` 末尾 `:root` | 唯一权威来源 |

**本轮已做（C1）**：`music.css` 里 4 条压缩行（22080 / 1848 / 518 / 418 字符）展开为
**2026 行**（42390 → 46570 字符）。展开用「只插入换行」的分词器完成，并经过两道机械校验：

1. `nows(before) === nows(after)`（去掉空白后的字符流逐字节相同）；
2. 浏览器 CSSOM 指纹完全一致 —— 317 条规则 / 连接后 `cssText` 长度 46013 / 同 hash。

> 教训：第一版自己写的「美化器」在注释边界处吞掉了 68 个规则块（459 → 410 个 `{`）。
> **纯格式化也必须用可证明无损的变换 + CSSOM 等价校验**，不能靠肉眼看 diff。

**未做（C2 全量）**：把音乐 App 层从 `app.css` 那个 1576 行的窄屏媒体查询里整体搬出来。
本轮只做了 B1 需要的那部分（见 §3），整体搬迁留作独立提交，理由见 §6。

---

## 3. 本轮实际改动（按阶段）

| 阶段 | 改动 | 关键证据（前 → 后） |
| --- | --- | --- |
| A | z-index 阶梯收口到 token | 桌面 drawer/mask/toast/modal：901/900/1000/1100 → 10006/10005/10010/10020 |
| A | 修复 `music.css` 里断掉的注释开头（曾静默吞掉 `#tab-netease`） | `#tab-netease` 在 CSSOM 中可见，padding `0.2rem 0` |
| A | `html` 里 `.pl-views` 后面的 `\`n` 断字 | `.pl-views` computed display = `flex` |
| B1 | 桌面/平板也能进全屏播放页 | 桌面点封面 `detailOpen` false → true；`.fs-wrap` 为 grid `280px 472px`，播放控件 elementFromPoint 全部命中 |
| B2 | 作品卡片首屏只渲染 12 张 + 「展开全部」 | 卡片 128 → 12；标签墙高 1396px(SE)/1012px(iPhone14) → 52px；网格 y1940 → y560(SE) |
| B3 | 真实时长懒加载（并发 3 + localStorage 缓存） | 「总时长 0:00」→ 实测 115:03 / 174:44，缓存条目随浏览增长 |
| B4 | 页脚移出 `#tab-player`；解锁区收进 `<details>` | 第三方音乐页签下页脚 0 个 → 1 个；解锁区 230px → 收起 48px / 展开 234–280px |
| D2 | bundle 收口 | `window.onclick` 覆盖式赋值 → `document` 委托；`startSpectrum()` 里错误的 `stallRetry=0` 移除（换曲时重置），卡死重试上限第一次真正生效；`6000/3` 收成 `STALL_CHECK_MS`/`STALL_MAX_RETRY` |
| D2 | 修 `music-app.js` 真 bug | `applyMode` 直接当 `DOMContentLoaded` 回调 ⇒ 收到 Event（truthy）⇒ 桌面端永远走 `init()` 分支而 `init()` 首行就 return。改为按类型判断 + 包一层 |

---

## 4. `!important` 债务

| 文件 | 条数 | 主要属性 |
| --- | --- | --- |
| `css/music.css` | 88 | font-size×16、padding×12、width×11、height×6、margin×5、gap×5、grid-template-columns×4 |
| `css/app.css` | 88 | display×17、width×8、max-width×7、height×6、padding×4 |
| `css/mobile.css` | 79 | max-width×8、box-sizing×5、padding×5、transform×5 |

**本轮已删**：`app.css` 中 4 条 z-index `!important`（层级遮蔽）—— 改用 token 后不再需要。

**下一步（可机械化）**：按 `display` 的 17 条先做。方法：临时去掉某条 `!important` →
六档跑一遍 → 对比目标元素的 computed 值与矩形。不动值 + 不动矩形 = 可安全删除。
这一步必须逐条实测，**不能靠选择器特异性推断**（本项目已经出现过「看起来冗余、
实际在窄屏阻止折行」的规则）。

---

## 5. 已证伪的诊断（别再照着修）

| 报告结论 | 实测 |
| --- | --- |
| 平台胶囊行被裁切、最后一个平台点不到 | `#nsPlatformChips` 是 `overflow-x:auto` 的横向滚动条，`scrollWidth 662` / `clientWidth 362`；程序化 `scrollLeft` 到 300 后最后一个 chip `iTunes` 命中正常 |
| toast 被侧边栏盖住 | toast 矩形 `[25,840,91,35]`，z 10010；正中心 `elementFromPoint` 返回 `#toastMsg` 本身；侧边栏 z 仅 95/96 |
| 页脚不见了 | 真因是页脚写在 `#tab-player` **内部**，切到第三方音乐页签才消失（desktop：内容止于 y561 / docH 900 → ~340px 空白）。已按 B4 修 |

---

## 6. 明确未做（及理由）

1. **C2 全量搬迁**：把 `app.css` 124–1699 行拆成「窄屏专属」与「两端共享」。迁移 1576 行
   一次性提交，回归面覆盖整个音乐 App 层；本轮已用「可证明无损」的方式处理了 B1 真正需要
   的那部分（多出的桌面规则与窄屏规则目前是**有意重复**，注释里写明了来源）。留作独立提交，
   届时同样用「CSSOM 指纹 + 六档矩形」双校验。
2. **D1 改用 `AppShell.adopt`**：`music-app.js` 自带的 `moved[]/take()/putBack()` 与
   `AppShell.adopt` 功能重叠。改写它要动「搬进/搬回」的全部路径，收益是代码整洁、
   风险是二级页在真机上搬错节点；本轮没有足够的真机手势覆盖，故保持现状并在此登记。
3. **看板娘**：按你的决定完全未动（不隐藏、不改 z-index、不改避让逻辑）。

---

## 7. 收尾验证（本轮最后一次全量）

- 六档视口：0 exception / 0 console error / 0 坏响应；
  `documentElement.scrollWidth - innerWidth ≤ 0`（无横向溢出）。
- 桌面与平板：`detailOpen=true`，`.fs-wrap` grid 两栏；`#playPauseBtn`、`#nextBtn`、
  `#volumeRange`、`#plDrawerOpen` 的 `elementFromPoint` 全部命中真实控件。
- 窄屏：全屏播放页与改动前逐像素一致（`.fs-lyrics` 高 173.86 → 174，`.fs-modes` 101）。
- 版本号：`?v=` 已随 `scripts/bump_version.py` 同步（Service Worker 预缓存键依赖它，
  不 bump 则老访客永远拿不到新 CSS）。
