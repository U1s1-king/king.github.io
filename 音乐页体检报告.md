# 音乐页体检报告（zhaokening.ccwu.cc）

生成时间：2026-09-18
范围：`D:\za\GitHub\king.github.io` 的 music.html 及其 JS/CSS 链路
方法：逐行读源码 + 对线上服务发真实请求实测 + 用户真机验证

---

## 0. 实测确认的线上依赖（全绿）

| 探测 | 结果 |
|---|---|
| `sakura-music-api.pages.dev/api/health` | 200 `{"ok":true,"mirrors":4}` |
| `?Range=bytes=0-1023` | **206**，`Content-Range: bytes 0-1023/11278814`，`Accept-Ranges: bytes` |
| `/api/search?keywords=周杰伦` | 200，277 条 |
| `data/playlist.json` | 200，13980 字节 |

Range 支持是 Worker **手工补的**（`cloudflare/music-api/_worker.js:731-810`），
因为源站 sakura-music.pages.dev 不实现 Range。没有它 `audio.seekable` 恒为 `[0,0]`。
**这条链路必须持续维护。**

---

## 1. 阶段 0 已完成（8/8）

| # | 问题 | 修复 | 文件 |
|---|---|---|---|
| 0.1 | **手机端无法调音量**（音量条留在被盖住的底层） | 改搬整个 `.extra-actions`，补 58 行全屏页版式 | `js/music-app.js:88`、`css/app.css:480-537` |
| 0.2 | toast 在播放页不可见 | z-index 1100 → **10010** | `css/app.css:1319` |
| 0.3 | 上传弹窗被播放页盖住 | z-index 1000 → **10020** | `css/app.css:1322` |
| 0.3b | **抽屉在播放页背后滑出**（新发现，改前即存在） | mask 900→10005，drawer 901→10006 | `css/app.css:1329-1332` |
| 0.4 | 拖进度条误切歌 / 整页平移 | `app-shell.js` 加控件豁免（`GESTURE_EXEMPT`） | `js/app-shell.js:159-206` |
| 0.5 | 点封面双绑、行为随机 | 摘掉 music-plus 的封面绑定，统一为「进全屏播放页」 | `js/music-plus.js:712-723,790` |
| 0.6 | 上传时长恒 0:00 + objectURL 泄漏 | 改为等真实 `loadedmetadata`（5s 兜底），试听 URL 用完即弃 | `js/music-bundle.js:750-828` |
| 0.6b | 歌单里的 blob 从不释放 | 新增 `releaseObjectUrl()`，删除/清缓存/解锁全接入 | `js/music-bundle.js:400-417,437,936,960,7481` |
| 0.7 | 快速切歌时长串位（A 的时长写进 B） | 闭包改用已捕获的 `s`，不再读 `currentIndex` | `js/music-bundle.js:571-582` |
| 0.8 | 进度条命中区仅 6–7px | 加透明 `::before` 撑到 **28px**（视觉不变） | `css/app.css:1312-1338` |
| 0.new | **选歌后抽屉不收回、列表重播动画**（用户真机发现） | 选歌后调 `window.sakuraPlDrawer.close()` | `js/music-bundle.js:532-548` |

### 真机验收结果（用户确认，2026-09-18）

- ✅ 播放页底部控件在**同一行**
- ✅ toast **有提示**
- ✅ 抽屉**可以滑出**（但选歌后不收回 → 已修）
- ✅ 上传弹窗**浮在最上层**

---

## 1b. 移动端 CSS 实测审计（headless Chrome，CDP 设备模拟）

方法：真实 Chrome 引擎 + `Emulation.setDeviceMetricsOverride`（390 / 320 / 480px，mobile:true），
读 CSSOM + 实测量盒 + `elementFromPoint` 命中测试。**不靠推断。**

| # | 缺陷 | 实测证据 | 修复 |
|---|---|---|---|
| **D1** | 🔴 `music.css` 第 3 行末尾的 \`n 吞掉了 `.pl-views` 规则 | 修复前 CSSOM 里 `hasPlViews:false`、计算值 `display:block; gap:normal; margin-bottom:0px` | 删掉那 2 个字节 → 计算值变 `display:flex; gap:9.6px; margin-bottom:12px`，CSSOM 300 条规则中 `.pl-views` 已存在 |
| **D2** | 🟠 `.track-title` 无溢出处理 | 74 字符曲名：`clientW 331 / scrollW 1000` → **溢出 669px** 被 `overflow-x:hidden` 硬裁，无省略号 | 加 2 行 `-webkit-line-clamp` + `overflow-wrap:anywhere` → 三个宽度下 **overflowX=0** |
| **D3** | 🟠 `.extra-actions` 的 nowrap 失效 | 第 1 行的 `.mode-strip{flex-wrap:wrap !important}` 压过第 64 行无 !important 的 nowrap；320px 下 `#speedBtn` 掉第二行，行高 **27.1 → 54.8px** | 给第 64 行补 `!important` → 三个宽度下 `flexWrap:nowrap`、三颗胶囊同一行、行高 **27px** |
| **D4** | 🟠 `.mode-chip` 触控目标仅 27px 高 | 实测 40x27 / 51x27 / 47x27，远低于 44px | 加透明 `::before` → 命中区**上下各外扩 23/21px（有效 71px）**，视觉仍 27px、行高不变 |
| **D5** | 🟡 `mobile.css` 加载顺序注释写错 | 注释称"本文件最后加载"，实际 `music.html:26-29` 里 **app.css 最后** | 已更正注释并说明为何这条是承重的（抽屉 z-index 提升依赖它） |

### 关于 D1 的教训（重要）

我**最初判断 D1 无害**，依据是"花括号配平 439/439"。这个判断是**错的**：
CSS 错误恢复会**静默吞掉整条规则**，而括号总数仍然平衡 —— 配平检查根本发现不了。

只有真实引擎能查到：修复前 `display:block`，修复后 `display:flex`。

### 被实测推翻的疑虑（不必再查）

- **看板娘 vs 二级页（9999 vs 9999）不是缺陷**：`.detail-view` 在 `#waifu-fab` **之后**插入 body，
  同权重按 DOM 顺序获胜；即使关掉避让，`elementFromPoint` 命中的也是 `.fs-wrap` 而非看板娘
- **无横向溢出**：三个宽度下 `documentElement.scrollWidth == innerWidth`
- **安全区全部正确**：bot-tab padding-bottom=34px、app-bar padding-top=47px、抽屉 46.8px、`.fs-wrap` 48px
- **频谱横线确实已修好**：`.spectrum-line` 无固定 height，横线在条下方，不穿条
- **320px 下 bot-tab 7 项放得下**
- 触控达标项：`.ctrl-btn` 48²、`#playPauseBtn` 62²、`.track-fav` 44²、`.pl-view` 44、`.ns-play/add/dl` 44²、`.fs-icon` 44²

---

## 2. 仍待处理（不在阶段 0 范围）

### MAJOR
- **Web Audio 风险**：`ensureAnalyser()` 建的 `audioCtx` **从不 close**；
  `music-bundle.js:188-189` 的注释说的「绝不会变静音」描述的是 `captureStream()`，
  但实现（`:237`）已改用 `createMediaElementSource()` —— **注释过时**，
  跨域不回 ACAO 时 AudioContext 会进入 tainted 状态并静音
- **stallRetry 上限失效**：`:296` 在 `startSpectrum` 里清零，而它在 play 事件链路上
  → `:324` 的 `>= 3` 永远不生效，卡死源会被无限重试
- **`window.onclick` 覆盖式赋值**（`:786`）：会顶掉其它脚本的同名赋值
- `music-app.js` 自造 `moved[]` 而不走 `AppShell.adopt()`（全站仅此一处）
- `setTimeout` 魔法数字（`music-app.js:299/312/318`）
- 看板娘避让 20 秒后永久停摆（`music-app.js:392-396`）

### 移动端视觉
- 歌词面板硬编码三档高度且不单调：152 → 100 → 106px
- `.extra-actions` 强制 nowrap，320px 屏卡边界
- 安全区只补了 `.pl-drawer`
- 频谱横线穿越（`.spectrum-line` 高度必须 auto，仅靠注释约束）
- `css/music.css` 前 3 行 2034/37/1850 超长压缩行 + 后续格式化内容混排
- `css/music.css` 第 3 行末尾混入 JS 转义符 `\`n`（字节 23936-23937，全站唯一）

### CSS 层叠现状（阶段 1 待治理）
同一选择器跨 `music.css` / `app.css` / `mobile.css` 多处定义，靠 `!important`
与源码顺序决胜：`.progress-bar-bg`(3处)、`.lyric-panel`(5处)、`.ctrl-btn`(4处)…

---

## 3. 层级现状（阶段 0 已打补丁，阶段 2 需重建）

| 层 | z-index | 备注 |
|---|---|---|
| 看板娘 / 大图 lightbox | 99999 | 最上层模态 |
| **上传弹窗** | **10020** | 阶段 0 抬升 |
| **toast** | **10010** | 阶段 0 抬升 |
| **歌单抽屉** | **10006** | 阶段 0 抬升 |
| **抽屉遮罩** | **10005** | 阶段 0 抬升 |
| 底部面板 `.mp-mask` | 10000 | |
| 详情层 `.detail-view` | 9999 | 桌面端遮罩 ::after 9998 |
| 底部 tab | 9998 | |
| 顶栏 | 9990 | |
| 迷你播放条 | 9980 | |

> 这套数字是硬编码分散在各文件里的，阶段 2 应统一成 token。

---

## 4. 安全（CF.txt）

| 凭据 | 状态 |
|---|---|
| `cfat_` 读取令牌 | 已失效，无需处理 |
| **`cfut_` 操作令牌** | **仍有效（高危）** |
| R2 秘钥 / JSONBin / ADMIN_KEY | 未验证 |

`cfut_` 能读 /user、/accounts、/zones（4 个 zone），且**未开 2FA**。
建议：① Roll 掉它；② 开 2FA；③ 轮换 R2 密钥；④ 该文件不要再明文存盘。

已修复（无需再管）：`access-control-allow-origin: *`（security headers v5 规则）。

---

## 5. 后续阶段

- **阶段 1**：CSS 治理 —— 展开压缩行、明确文件归属、出层叠表、删死规则、建 z-index token
- **阶段 2**：-导航统一 —— `AppShell.adopt()` 收敛、歌词页合并、手势分区通用化
- **阶段 3**：视觉打磨（对齐网易云）—— 全屏页版式、进度条重做、歌单 sheet 增强、骨架屏
- **阶段 4**：真机多机型验证（320 / 390 / 414 / 428 + iOS Safari / Android Chrome）

---

## 6. 已确认 SAFE（不必再查）

跨 IIFE 无 ReferenceError（31 个 typeof 守卫全部有声明）· 被搬迁节点的缓存引用安全 ·
歌词节点每次现查 · 歌单列表事件委托免疫 innerHTML 重建 · 搜索结果绑定随节点重建 ·
`.ns-platform` 链路未被动 · 歌词 token 竞态已处理 · audio error 防重入 ·
playFailCount 上限有效 · LRC 解析正则正确 · 歌词自动滚动重试 · LyricHelper 定时器不叠加 ·
频谱 rAF 防重启动 · 搜索并发分组顺序 · IndexedDB 封装正确 · 下载路径 objectURL 已 revoke ·
music-api 下载不重复 · putBack 逆序归还 · teardown 拆装完整 · 视口跨越只广播一次 ·
prefers-reduced-motion 检测 · 搜索历史 XSS 转义 · MediaSession try/catch 包裹
