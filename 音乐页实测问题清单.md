# 音乐页实测问题清单（供讨论，未修改任何代码）

实测时间：本轮会话
方法：本地 `node server.js`（127.0.0.1:8888）+ 真实 Chrome 153 无头引擎，
CDP `Emulation.setDeviceMetricsOverride` 设备模拟 + `Input.dispatchTouchEvent` 真实点按 +
`document.elementFromPoint` 命中测试 + CSSOM 读取。**全部结论有实测数据，无推断。**

实测设备：desktop 1440x900 / iPad 820x1180 / iPhone14 390x844 / iPhoneSE 320x568 / iPhonePlus 428x926

---

## 0. 先说结论：体检报告与实际不符

`音乐页体检报告.md` 声称阶段 0 的 8 项全部完成、真机验收通过。**实测不符：**

| 报告声称 | 实测结果 |
|---|---|
| 0.1 手机端可调音量 ✅ | 桌面端/iPad 上音量条**被看板娘 canvas 吞掉点击**（见 A1） |
| 0.3 上传弹窗 z-index 10020 ✅ | 仅 **≤768px** 生效；桌面/iPad 仍是 **1000**（见 A3） |
| 0.3b 抽屉 mask 10005 / drawer 10006 ✅ | 仅 **≤768px** 生效；桌面/iPad 仍是 **900 / 901**（见 A3） |
| 0.2 toast 10010 ✅ | 仅 **≤768px** 生效；桌面端仍是 **1100**，且**压在左侧栏下面**（见 A2/A4） |
| 0.4 拖进度条误切歌已修 | 桌面端命中的是 canvas，**根因（看板娘）没治** |
| 0.new 选歌后抽屉收回 | 未复测 |

根因：`css/app.css` 的这套 z-index 修复写在 **第 128 行开启的 `@media (max-width: 768px)` 块内**（该块从 128 行一直延伸到 1728 行，1357-1362 行的抽屉、1347-1352 行的 toast/modal 都在里面）。
桌面端与 iPad（820px）**一行都不匹配**。

---

## A. 阻断级（功能点了没反应）

### A1 🔴 看板娘 canvas 吃掉播放控件点击 —— 桌面端 & iPad

`#live2d` canvas 是 **360x360**，`pointer-events: auto`，`#waifu-fab` 层 300x420 / `z-index 9999`。

实测 `elementFromPoint` 结果（desktop）：
```
#nextBtn      -> CANVAS#live2d  (46x46 @1143,585)
#volumeRange  -> CANVAS#live2d  (72x4   @1188,645)
#plDrawerOpen -> CANVAS#live2d  (97x29  @1308,645)
```
iPad 上更多：`#playPauseBtn` / `#nextBtn` / `#speedBtn` / `#volumeRange` / `#plDrawerOpen` / `.progress-bar-bg` 六个全被吞。

原因：手机端的豁免靠 `html.waifu-dodge #waifu-fab { pointer-events: none }`（mobile.css:423），
而 `waifu-dodge` **只在窄屏由 `js/music-app.js` 的 `bindWaifuDodge` 添加**；桌面/iPad 上 `html` 类名实测只有 `app-ready`，**没有 dodge**。
另外 `js/live2d-app.js` 第一行就 `if (!narrow()) return;`，桌面端完全不介入。

→ 桌面端「下一首 / 音量 / 歌单」这三个控件，**鼠标点上去完全无效**。

### A2 🔴 平台选择器在手机端宽度为 0，搜索无法选平台

实测 iPhone14：`#ns-platform { w: 0, h: 0 }`
`css/app.css:594` 的 `.ns-platform { display: none }` 在窄屏把它藏了，
`js/music-app.js:174` 另外插了一排 `.ns-chip` 代替。

但这排 chip **横向溢出 300px 且被裁掉**：
```
rowW=362  clientW=362  scrollW=662   最后一项右边界=675px（屏幕 390px）
```
裁它的是 **`body { overflow-x: hidden }`**（`docW 390 == winW 390`，所以页面看不出横向滚动条，
最后 3 个平台「咪咕 / B站 / iTunes」**直接点不到，也滚不到**）。

### A3 🟠 层级修复桌面端无效（连带 A2 的卡片视图）

实测 z-index（同一页面、不同视口）：

| 元素 | desktop 1440 | iPad 820 | iPhone14 390 |
|---|---|---|---|
| `.pl-drawer` | **901** | **901** | 10006 |
| `.pl-drawer-mask` | **900** | **900** | 10005 |
| `#uploadModal` | **1000** | **1000** | 10020 |
| `#toastMsg` | **1100** | — | 10010 |

桌面端详情层是 9999（`app-shell.js:250` 内联），**抽屉 901 / 弹窗 1000 / toast 1100 全在它下面**。
→ 桌面端点「上传本地音乐」打开弹窗，会被详情层盖住 = 用户看到「点了没反应」。

### A4 🟠 `#uploadTrigger` 在任何视口都点不到

实测三个设备一致：`#uploadTrigger -> null.null`（`elementFromPoint` 返回 `null`）。
它在 `.pl-drawer` 里，而抽屉**默认关闭且在屏幕外**（desktop `x=1447` > 视口 1440）。
这不是 bug，但说明「上传入口」只有抽屉打开后才能用，主页面**没有可见的上传入口**。

---

## B. 手机端布局

### B1 🔴 iPhone SE 上整个播放器被底栏盖住

```
bot-tab          y=512   h=56   z=9998   (fixed)
extra-actions    y=760            ← 屏幕高只有 568
controls-row     y=674
lyric-panel      y=532   ← 已被 bot-tab 压住
```
320x568 下 `.music-container` 高 **1200px**，播放器从 y=303 一直排到 y=860。
用户**必须滚动**才能碰到播放控件，且滚动中途 `bot-tab` 一直压着 `lyric-panel`。
截图 `_shot_iphoneSE.png` 可见：首屏只有封面 + 标题，播放按钮全在屏幕外。

### B2 🟠 `.extra-actions` 在 320px 与 428px 上换行，行高翻倍

```
iPhone14 (390): extra-actions 331x27   三颗胶囊同一行 ✅
iPhoneSE (320): extra-actions 261x44   mode-strip@y769, plDrawerOpen@y760  ← 错位
iPhonePlus(428): extra-actions 369x76  mode-strip 高 76px（胶囊被拉成 76px 高）❌
```
iPhonePlus 上 `#speedBtn` 实测 **49x76 / 75x76**，胶囊被纵向拉伸——`.mode-chip` 没有固定高度，
而 `.extra-actions` 在 428px 命中了不同的 flex 规则。

### B3 🟠 桌面端「第三方音乐」页下方有 342px 空白

实测 desktop：`unlock-section` 底部之后还有 **341.98px** 空白（`docH 900 == viewH 900` 撑满视口但内容只到 558px）。
原因：`.unlock-section { margin-top: 1.6rem }` + 卡片视图/统计栏的 `margin-bottom` 在 `#ns-view-search` 隐藏后仍残留。

### B4 🟡 卡片视图「分类过滤」在手机端堆成 1012px 高

实测 iPhone14 `.filter-tabs { h: 1012px, scrollW: 443 > clientW 362 }`，
`works-grid` 被推到 **y=1517**（首屏 844px 完全看不到作品）。
128 个作品、48 个艺术家全部平铺成 chip，手机上就是一面「标签墙」。
截图 `_cards_iphone14.png` 即为实况：滚了 1.5 屏还在选艺术家。

### B5 🟡 全屏播放页歌词区 174px 硬编码 + 模式行溢出

实测 iPhone14 全屏页：`.fs-lyrics { h: 174 }`（`flex:1 1 auto; min-height:120px`），
`.fs-modes { h: 101 }`，三颗胶囊「单曲循环 / 随机播放 / 倍速」被换成**竖排三行**（截图可见），
右侧音量条与「歌单 128 首」挤在同一行，`歌 128 首` 换行成两行。

### B6 🟡 toast 位置不当（桌面端压在侧边栏下）

实测 desktop：`toast { x:25, y:840, w:91, h:35 }`，而 `.site-sidebar` 是 `x:0, w:220, position:fixed`。
→ toast **完全落在侧边栏区域内**，被栏体盖住（截图 `_b_b_desktop_netease-search.png` 左下角可见半截「测试提示」）。
手机端 `x:25` 没问题，但桌面端 `left:25px` 是写死的，没考虑 220px 侧栏。

---

## C. 数据 / 功能正确性

### C1 🟠 卡片视图「总时长」恒为 0:00

实测 desktop 与 iPhone14 一致：`statsText = "作品总数 128 艺术家 48 总时长 0:00"`。
128 首作品的时长字段没被累加（`data/playlist.json` 里的 duration 没进 `statsTotalDuration`）。

### C2 🟡 「清空缓存」文案是多出来的一个空格 + 竖排

实测 `clearCache.textContent === " 清空缓存"`（前导空格）。
需要确认是否与 `.cherry-tip` 的排版意图一致。

### C3 ✅ 无 JS 异常、无 404

五个视口全部：`exceptions=0  failed=0  badResp=0`。
唯一 console error 是 `navigator.vibrate` 被浏览器策略拦截（`js/common.js`，非缺陷）。

---

## D. 代码结构（影响后续维护，不直接影响用户）

| # | 问题 | 实测 |
|---|---|---|
| D1 | `css/music.css` 第 1 行是 **22040 字节的单行压缩 CSS**（267 条规则），第 3 行 1848 字节 | 无法 diff / review |
| D2 | 第 1 行**混入 JS 转义符 `\n`**（报告 D1 说已删，实测仍在 23936-23937 字节处） | 需再确认 |
| D3 | `css/app.css` 单个 `@media (max-width:768px)` 块从 **128 行延伸到 1728 行（1600 行）** | 所有层级修复被关在里面 |
| D4 | 同一选择器跨 `music.css` / `app.css` / `mobile.css` 多处定义，靠 `!important` + 源码顺序决胜 | `.progress-bar-bg`(3处)、`.lyric-panel`(5处)、`.ctrl-btn`(4处) |
| D5 | z-index 硬编码分散：9999 / 10000 / 10005 / 10006 / 10010 / 10020 / 99999 | 无 token |

---

## 建议的修改优先级（待你定夺）

**第一梯队 —— 点了没反应的，必须修：**
1. A1 看板娘遮挡：给 `#waifu-fab` / `#live2d` 一个**全视口生效**的命中豁免（不是只在窄屏 dodge 时）
2. A2 平台 chip 溢出：让 chip 行真正可横向滚动（解除 `body overflow-x:hidden` 的裁剪）
3. A3 层级修复移出 `@media(max-width:768px)`

**第二梯队 —— 手机端版式：**
4. B1 iPhone SE 首屏排布（播放器要进首屏）
5. B2 `.extra-actions` 320/428 换行
6. B4 卡片视图分类过滤折叠

**第三梯队 —— 打磨与结构：**
7. B6 toast 位置、B3 空白、C1 总时长
8. D1/D3 CSS 展平与归属整理

---

## 待你决定的问题

1. **桌面端还要不要「全屏播放页」？** 实测桌面点封面 `detailCount=0`（`allowDesktop:true` 在 `music-plus.js:454`，但 `app-shell.js:225` 仍拦了）——桌面端其实**没有**二级页。那 A3 的层级问题在桌面端是不是伪命题？
2. **看板娘要不要在音乐页整体隐藏？** 它是遮挡的根源；纯装饰 vs 功能控件，取舍请你定。
3. **卡片视图「作品卡片」这个 tab 留不留？** 128 作品全平铺、总时长恒 0:00、手机端 1.5 屏才看到作品——是否符合你的预期？
4. **本地音乐解锁**（.ncm/.qmc/…）这个功能你还在用吗？如果不用，可以从音乐页移除，页面能瘦一大截。
